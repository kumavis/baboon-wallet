{
  "name": "baboon-wallet",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd src && beefy index.js:bundle.js --open",
    "build": "mkdir build && browserify src/index.js > build/bundle.js && cp src/index.html build/",
    "deploy": "ghpages -p build"
  },
  "devDependencies": {
    "beefy": "^2.1.8",
    "browserify": "^16.5.1",
    "ghpages": "^0.0.10"
  },
  "dependencies": {
    "circomlib": "^0.2.3",
    "hat": "^0.0.3",
    "snarkjs": "0.1.20"
  }
}
